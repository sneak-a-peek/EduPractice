#include <stdio.h>

int main() {
    int n;
    int q = 1;
    int i;
    int j;
    int A[i][j];
    int S[i];


    for (i = 1; i < n; i++)
        int S[i] = {0};
        for (j = 1; j < n; j++)
            int A[i][j];
            S[i] = S[i] + A[i][j];
        if (S[q] < S[i])
            q = 1;

    printf("%d\n", q);

    return 0;
}
