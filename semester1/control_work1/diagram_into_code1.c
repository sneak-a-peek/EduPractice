#include <stdio.h>

int main() {
    int n;
    int q;
    int i;
    int M[i];
    int X[i];
    n = 12;
    //scanf("%d\n", n);
    for (i = 1; i < n; i++)
        int X[i];
    for (i = 1; i < 10; i++)
        int M[i];
    q = 0;
    for (i = 1; i < n; i++)
        M[X[i]] = M[X[i]] + 1;
    for (i = 1; i < 10; i++)
        if (M[i] > 1)
            q = q + 1 * M[i];
            printf("%d\n", q);

    return 0;
}
